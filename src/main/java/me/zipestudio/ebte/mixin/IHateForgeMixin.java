package me.zipestudio.ebte.mixin;

import net.minecraft.client.Minecraft;
import org.spongepowered.asm.mixin.Mixin;

// I hate Forge... Do not delete this shit.
// No-op stub so the Mixin AP generates the refmap Forge's reobfJar needs.
// Not referenced in any loader's mod metadata, so it never applies at runtime.
@Mixin(Minecraft.class)
public class IHateForgeMixin {
}